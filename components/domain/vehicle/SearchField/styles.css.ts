import { style } from '@vanilla-extract/css'

export const container = style({
    position: 'absolute',
    top: '2rem',
    right: '2rem',
    display: 'flex',
    gap: '12px',
    alignItems: 'center',
    marginBottom: '12rem',
})
