import { Skeleton } from '@mantine/core'

const HeaderSkeleton = () => {
    return (
        <div>
            <Skeleton height={40} mt={5} width='100%' radius='md' />
        </div>
    )
}

export default HeaderSkeleton
